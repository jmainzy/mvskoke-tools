[build]
  base = "frontend/"
  command = "ng build"
  publish = "frontend/dist/mvskoke-demo-site/browser"

[dev]
  command = "uvicorn app:app --reload"
  port = 8000
  targetPort = 8000

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200